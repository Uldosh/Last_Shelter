plugins {
    id 'base'
}

allprojects {
    group = 'com.lastshelter'
    version = '0.1.0'
}

subprojects {
    apply plugin: 'java'

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
        options.release = 17
    }
}
